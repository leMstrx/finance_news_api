beautifulsoup4==4.12.2
requests==2.31.0
transformers==4.38.0
torch==2.2.0
sqlite3==3.37.0  # Note: sqlite3 is included with Python's standard library, so it's not installed via pip and doesn't need to be in requirements.txt
schedule==1.2.1
Flask==3.0.2